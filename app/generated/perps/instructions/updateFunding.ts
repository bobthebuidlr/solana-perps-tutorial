/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { PERPS_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_FUNDING_DISCRIMINATOR = new Uint8Array([
  224, 66, 9, 70, 75, 81, 94, 88,
]);

export function getUpdateFundingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_FUNDING_DISCRIMINATOR,
  );
}

export type UpdateFundingInstruction<
  TProgram extends string = typeof PERPS_PROGRAM_ADDRESS,
  TAccountMarkets extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarkets extends string
        ? WritableAccount<TAccountMarkets>
        : TAccountMarkets,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateFundingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UpdateFundingInstructionDataArgs = {};

export function getUpdateFundingInstructionDataEncoder(): FixedSizeEncoder<UpdateFundingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UPDATE_FUNDING_DISCRIMINATOR }),
  );
}

export function getUpdateFundingInstructionDataDecoder(): FixedSizeDecoder<UpdateFundingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUpdateFundingInstructionDataCodec(): FixedSizeCodec<
  UpdateFundingInstructionDataArgs,
  UpdateFundingInstructionData
> {
  return combineCodec(
    getUpdateFundingInstructionDataEncoder(),
    getUpdateFundingInstructionDataDecoder(),
  );
}

export type UpdateFundingAsyncInput<TAccountMarkets extends string = string> = {
  markets?: Address<TAccountMarkets>;
};

export async function getUpdateFundingInstructionAsync<
  TAccountMarkets extends string,
  TProgramAddress extends Address = typeof PERPS_PROGRAM_ADDRESS,
>(
  input: UpdateFundingAsyncInput<TAccountMarkets>,
  config?: { programAddress?: TProgramAddress },
): Promise<UpdateFundingInstruction<TProgramAddress, TAccountMarkets>> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    markets: { value: input.markets ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.markets.value) {
    accounts.markets.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 97, 114, 107, 101, 116, 115]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.markets)],
    data: getUpdateFundingInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateFundingInstruction<TProgramAddress, TAccountMarkets>);
}

export type UpdateFundingInput<TAccountMarkets extends string = string> = {
  markets: Address<TAccountMarkets>;
};

export function getUpdateFundingInstruction<
  TAccountMarkets extends string,
  TProgramAddress extends Address = typeof PERPS_PROGRAM_ADDRESS,
>(
  input: UpdateFundingInput<TAccountMarkets>,
  config?: { programAddress?: TProgramAddress },
): UpdateFundingInstruction<TProgramAddress, TAccountMarkets> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    markets: { value: input.markets ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.markets)],
    data: getUpdateFundingInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateFundingInstruction<TProgramAddress, TAccountMarkets>);
}

export type ParsedUpdateFundingInstruction<
  TProgram extends string = typeof PERPS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    markets: TAccountMetas[0];
  };
  data: UpdateFundingInstructionData;
};

export function parseUpdateFundingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateFundingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { markets: getNextAccount() },
    data: getUpdateFundingInstructionDataDecoder().decode(instruction.data),
  };
}
